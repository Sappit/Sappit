<!-- <nav class="navbar navbar-dark navbar-expand clientside-filter">
  <form class="form-inline">
    <input
      class="mr-sm-2 form-control-sm"
      type="text"
      name="text"
      placeholder="Filter Clientside"
      on:change={onChange}
      value={value.text}
    />
  </form>
</nav>
 -->
<script>
/*
import get from 'lodash/get';
import { startMinWait } from '~/lib/sleep';
import { createEventDispatcher } from 'svelte';
import validatePropObject from '~/lib/validateProp/object';

const dispatch = createEventDispatcher();

const keys = ['text'];

// props
export let value;

$: validatePropObject(value);

function onChange ($event) {
  const key = $event.target.name
  const newValue = $event.target.value
  if (value[key] !== newValue) {
    value[key] = newValue;
    value = value;

    dispatch('input', value);
  }
}
*/
</script>
